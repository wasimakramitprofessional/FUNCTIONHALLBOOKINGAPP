import React from "react";

function FilterPanel(){
    return (
        <div className="filter-panel">
            <span>Filters: Locality ^ | Capacity ^ | Price ^ | Amenities ^ | Rating ^</span>
            <span>Sort: Recommended ^</span>
        </div>
    );
}

export default FilterPanel;